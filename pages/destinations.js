import React from 'react';

import data from '../src/Data/data.json';
const Destinations = () => {
  console.log(data);
  return (
    <div>
      <h1>DESTINATIONS</h1>
    </div>
  );
};

export default Destinations;
